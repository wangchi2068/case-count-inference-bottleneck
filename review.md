# 同行评审意见：《病例规模不再是主要推断瓶颈的边界：估计目标、监测杠杆与流感住院实证》

评审对象：https://github.com/wangchi2068/case-count-inference-bottleneck （GitHub 仓库，含 `paper/main.tex`、`paper/main.pdf`、`paper/main_docx.docx`、`theory/propositions_and_proofs.md`、`simulations/` 四个脚本、`data/` CDC NHSN 原始 CSV、`figures/` 四张图）。

核验方式：仓库可正常克隆（未出现 403/404，无不可达源）。评审重跑了 `simulations/run_empirical_flu_analysis.py` 并与仓库内 `simulations/empirical_stats.json` 逐字段比对；调用 `run_detection_simulations.run_detection_experiments()` 取回其内部统计量；按论文自述规则独立重写了功效枚举、预测模拟核心循环和若干理论公式的数值验证；对 `main_docx.docx` 做了解包结构检查；并用 Crossref 核对了关键参考文献。

---

## 一、概述（Summary）

作者提出一个“分层随机推断框架”，区分两类估计目标：（A）当期环境条件下的传播参数 $R_t$；（B）跨期基础水平 $\bar{R}$。在已知风险集 $n$、固定报告率 $\rho$ 的负二项分支--二项报告基准模型下，论文给出六个命题：$C_t$ 在给定 $(n,R_t)$ 下严格服从 $\mathrm{NB}(nk,\ k/(k+\rho R_t))$，矩估计量 $\widehat R_t=C_t/(\rho n)$ 为条件 MLE 且方差达到 Fisher 信息逆（命题 1）；对目标 B 的单期估计方差为 $v_R+\bar R/(\rho n)+(\bar R^2+v_R)/(kn)$（命题 2）；由此定义规模项与环境项相等的“方差占比交叉点” $m_\times=A/v_R$（定义 1）；给出 $n$ 与 $\rho$ 两条杠杆的非对称性（命题 3）；在 $B=T\cdot m$ 预算约束下推导 $v_R/T+A/B$ 与 AR(1) 的闭式（命题 4）；给出负二项单调似然比精确增长判定检验（命题 5）；以及单步预测方差的全方差恒等式（命题 6）。

实证部分使用 CDC NHSN 2020-08 至 2026-09 的 51 个辖区、16,218 个州--周流感住院观测，构造 $D_t=\sum_s w_s C_{t+1-s}$（$w=(0.65,0.25,0.10)$）作为报告历史代理，以 $\widehat C_{t+1|t}=\bar R_{\text{phase}}D_t$ 为基准评分，得到：初期波段仿射拟合 $a=0.0991$、$b=2.2434$（州聚类 bootstrap 95% CI $[1.5019,3.0161]$）、经验交叉点 $b/a=22.6$（截尾 1% 后 10.6）；误差预算分解为“与规模无关 54.6% / 规模项 45.1%”；达峰期以平稳基准预测时总误差恒在 0.292-0.312，其中基准偏移占总体损失 88.9%，但控制偏移后规模项仍显著为正（$b=7.9272$）；报告制度切换（自愿填报期覆盖率 54.6%）伴随增长率标准差扩张 192%、偏度扩张 355%，而 IQR/MAD 基本不变；全国汇总序列初期 RelMSE 降至 0.0348（$N=28$）。

论文的写作定位相当克制：多处主动声明“条件是已知 $n$、已知 $\rho$、模型正确设定”“$m_\times$ 不是最低可用病例数”“不宣称发现通用信息论硬极限”。这种自我设限在同类稿件中并不常见，是本稿的主要亮点之一。

## 二、优点（Strengths）

**S1. 命题 1-4、6 的数学是正确的，我逐条复核过。** 命题 1 的 PGF 复合、MLE 求解（$\partial \ln L/\partial R_t=k(C_t-\rho nR_t)/[R_t(k+\rho R_t)]$）、得分方差等于 $n/(R_t/\rho+R_t^2/k)$ 三步我都独立重算，与文中一致；命题 2 的全方差分解、命题 3 的恒等拆解 $\bar R/(\rho n)=\bar R/n+(1-\rho)\bar R/(\rho n)$、命题 4 的 AR(1) 闭式 $v_RT[(1+r)/(1-r)-2r(1-r^T)/(T(1-r)^2)]$（我用 $T=10,r=0.5$；$T=5,r=0.8$；$T=50,r=0.3$ 两种方式对拍，逐位吻合）均正确。命题 4 中 $AP(1)=\lim$ 的“报告率饱和下界”与 $AP(2)$ 的“$n\to\infty$ 全域收敛”也确实分别来自 $1/(\rho n)$ 项与 $1/(kn)$ 项的系数结构——$\rho$ 只能消掉抽稀方差，$k$ 引入的分支过离散项与 $\rho$ 无关，这一非对称性是真实的。

**S2. 实证管线的确定性可复现性很好。** 我重跑 `run_empirical_flu_analysis.py` 后与仓库内 `empirical_stats.json` 逐字段比对，只有 18 个字段不同，且全部是浮点末位差异（例如 `fgls_sep.b_early` 2.2434251194418398 对 2.2434251194418384），没有任何量级差异。§5 的每一个头条数字我都从原始 CSV 重新算过并全部对上：16,218 / 10,303 个样本、三阶段 $N=6009/420/3874$ 与覆盖率 91.1%/54.6%/91.8%、$a=0.0991$、$b=2.2434$、CI $[1.50,3.02]$、$m_\times=22.6$、截尾 1% 后 10.6、十分位拟合 22.6、IV 对照 $b=2.5305/a=0.0841$、达峰期 $N=1220$、偏移份额 88.9%、预算份额 54.6%/45.1%、大规模端 0.104 对全体 0.181、滚动中位数 36.5（IQR $[14.2,61.6]$）、流感季/非流感季分离 20.4 对 46.9、全国 6662 例与 RelMSE 0.0348。我没有发现任何“编造数字”。

**S3. 达峰期结论的自我纠正是本稿最值得肯定的一处。** `main.tex` 表 3 与相关段落明确指出：达峰期 $b=7.9272$ 与初期 $b=2.2434$ 分母不同、不可直接比较，共同分母口径下达峰期反而更低（1.8521 对 2.2434），并写道“原稿若以各自归一化口径直接比较……会得出‘达峰期规模项绝对量更高’的相反印象，那是分母差异的伪象”；联合交互项 CI $[-2.2702,0.6261]$ 含零，结论是“未发现达峰期斜率增强，也不能断言其更低”。主动报告并撤销一个对自己有利的比较，是罕见的诚实。

**S4. 主动报告不利的稳健性结果。** 回顾性基准 vs 滚动起点基准的落差（$a$ 从 0.099 升到 0.22-0.41、$b$ 从 2.24 升到 9.16-13.27）、截尾 1% 使交叉点从 22.6 左移到 10.6、自愿填报期 IQR 与 MAD 并未扩张（0.638→0.594、0.298→0.299）、离季滚动窗口估计“个别窗口逾千例”、重叠窗口使 IQR 有效样本量小于表观窗口数——这些都写进了正文，没有被藏起来。

**S5. 命题 6 的“事后统计量与事前预测的代数同一性”是一个干净且有用的观察。** $\mathrm{RelMSE}=\mathbb{E}[(G_{t+1}-\bar R)^2]/\bar R^2$ 使两个评价对象在翻倍削减率上严格同一，作者没有借此把“预测评估”包装成两套独立证据，反而用式 (exact_renewal_decomp) 说明它与单代基准之间还差一个预测偏差项和一个随机分母重加权项。这个等式我核对无误。

## 三、缺点（Weaknesses）

**W1（严重）预测模拟中存在未披露的、依赖数据的截尾，且它恰好决定了论文要展示的那一点。** `simulations/run_forecasting_crossover_simulations.py:88` 是 `valid_growth = [e for e in growth_errs if e < 5.0]`。这行把平方增长率误差中所有 $\ge 5$ 的样本直接丢弃。我按原参数重跑核心循环，量化了后果：

| 规模组 | 评价点 | $D_t=0$ 剔除 | $e\ge 5$ 剔除 | 未截尾均值 | 截尾后均值 | 参照值 |
|---|---|---|---|---|---|---|
| $m=6$ | 3900 | 3 | **38** | **0.7176** | 0.3131 | 0.3939 |
| $m=12$ | 3900 | 1 | **7** | **0.1903** | 0.1654 | 0.2169 |
| $m\ge 25$ | 3900 | 0 | 0 | 同左 | 同左 | — |

正文 §4.2 明确说“我们明确规定评估仅在具备活跃历史信息的周（$D_t>0$）上进行”，图注的样本核算也只列出 3/1/0 三个剔除数；读者据此会认为 $m=6$ 组的 3,897 与 0.313 之间只差一次 $D_t=0$ 筛选。实际上均值被压低了 2.3 倍（0.7176→0.3131），而正文把 $m=6$ 处“经验 0.313 对参照 0.394”的差距归因于“动态风险集的时序漂移、随机分母加权及活跃周的截断筛选”——归因错误，真实原因是这行未披露的截尾。这个截尾还准确地作用于“小 $m$、重尾、正是论文要论证交叉点所在”的区域，方向上系统性地让经验曲线更贴合理论参照曲线（未截尾时 $m=6$ 的相对损失为 0.5425，是缩放参照 0.2978 的 1.8 倍，“全尺度平滑追踪”的说法在完整数据下不成立）。此外 `:72` 的 `I[t] = max(1, I_t)` 把感染数强制下界为 1，同样未在正文说明。**必须删除该截尾（或改为引言中明确声明的、非数据依赖的 Winsorize 阈值并同时报告未截尾结果），并在正文与图注中如实列出全部剔除规则。**

**W2（严重）Word 交付件与 LaTeX 权威版在实质结论上互相矛盾，且 Word 版保留了 LaTeX 已明确推翻的那个错误论断。** `paper/main_docx.docx` 的达峰期段落写的是：“但本文的检验不支持‘达峰期病例规模不再有用’这一更强主张——控制基准偏移后规模相关方差项仍显著为正**且绝对量高于初期**”。这正是 `main.tex`:536/555 与表 3 判定为“分母差异的伪象”的那个比较。Word 版同时删掉了表 3（三口径比较）、达峰期分组表、滚动起点核验、图 1 与全部尺度统一的讨论，因此读 Word 版的人只会读到被纠正前的强论断。同类越界还有三处：Word 版称滞后窗口基准为“滞后**外生**基准”并断言分离“并非基准内生性的产物”（LaTeX 说“滞后只保证时间先后，不自动保证外生性”）；Word 版断言工具变量“排他性依赖当周填报冲击不跨周持续，NHSN 周度独立填报机制与此相容”（LaTeX 说排除限制未获验证）；Word 版把跨州汇聚的机制写成已确立的因果解释（LaTeX 说“本对比未单独识别环境抵消、子代波动平均与其他汇总效应的相对贡献”）。README 自称 docx 是“按学报模板派生的交付件，修订时以 main.tex 为准”——那就必须让两个交付件在结论上一致。**在 Word 版回填表 3 与尺度统一说明，或把 Word 版的达峰期结论改回 LaTeX 的表述。**

**W3（重要）误差预算的“环境底板 54.6%”在论文自己的更新过程代数下并未被识别为“环境”项。** 用论文自己的模型（$I_{t+1}\mid\Lambda,R\sim$ 均值 $\Lambda R$、方差 $\Lambda R+\Lambda R^2/k$；$C\mid I\sim\mathrm{Bin}(I,\rho)$；$\Lambda$ 在 $\mathcal F_t^C$ 下不可观测，均值 $\bar\lambda$、方差 $v_\Lambda$）把论文式 (exact_renewal_decomp) 中的 $\sigma^2_{t+1|t}/D_t^2$ 展开到 $D_t\approx\rho\bar\lambda$，得到

$$\frac{\mathrm{Var}(C_{t+1}\mid\mathcal F_t^C)}{D_t^2}=\frac{A}{m}+\Big[v_R+\frac{v_\Lambda}{\bar\lambda^2}\,(\bar R^2+v_R)\Big].$$

第一项与论文的 $A/m$ 完全一致（这是对论文参照曲线的一个独立支持，值得肯定）；但**常数项并不等于 $v_R$**，而是“环境方差 + 未观测感染压力 $\Lambda$ 的不确定性”。代入论文自述的住院参数（$\bar R_{\mathrm{early}}=1.6706$、$v_R\approx0.04$--$0.08$），仅由环境贡献的截距应为 $v_R/\bar R^2\approx0.014$--$0.029$，而估计值是 0.0991，相差 3--7 倍；这个差额可以由“代理不确定性”吸收（对应 $\Lambda$ 的变异系数约 0.29，对更新过程完全合理）。同理，机制隐含的规模项 $(\bar R+\rho(\bar R^2+v_R)/k)/\bar R^2$ 在论文自述的 $\rho\approx0.01$--$0.05$、$k\approx0.2$--$0.5$ 下只有约 0.6--0.8，而估计值是 2.2434。**结论：把截距 $a$ 读作“与规模无关的**环境**误差、因而提示环境协变量的价值”，在论文自己的机制下是欠识别的**——其中至少一部分是状态/代理不确定性（可以用论文自己引用的半机制状态空间模型削减）或不可约的机制噪声，而不是“加协变量就能拿回来”的部分。建议：(a) 在 §5.2 明确区分 $v_R$ 与 $v_\Lambda$ 两项并给出可识别方案；(b) 或在正文中把 $a$ 改称“与规模无关项（含环境与代理不确定性）”，并把“环境协变量价值”的政策推论降级为“待识别”。

**W4（重要）54.6%/45.1% 的份额分解依赖于最不可靠的那套基准。** 论文自己报告：换成事前口径的滚动起点基准后，$a$ 从 0.0991 升到 0.2217/0.3409/0.4063，$b$ 从 2.24 升到 9.16/9.46/13.27（4-6 倍变化）。份额分解是 $a$ 与 $b/D_t$ 在样本分布上的代数摊派，因此在诚实基准下份额结构会大幅改变。论文在正文里承认“回顾性基准确实偏乐观”，但摘要、README 与 Word 版仍把“54.6%/45.1%”和“六成”当作首要结论抛出。**建议把滚动口径的份额也列进摘要或至少列进同一张表，让读者看到份额本身的不稳定性。**

**W5（重要）§5.4 的“流感季子样本校准”没有校准掉它要校准的混杂。** P2（自愿填报期）落在流感季月份的观测全部是 2024 年 10 月（55 个州--周，均值增长率 2.155，而 P1/P3 的流感季池分别是 1.143/1.226）——这是 2024-25 季的**起峰阶段**，而 P1/P3 的流感季池是 10 月至次年 3 月的混合。把“单个起峰月”与“24 个流行月”比标准差（4.793 对 0.652），比的是疫情阶段而不是报告制度。另外，`figures/fig4` 面板 (d) 用 `showfliers=False` 绘图，视觉上 P2 的箱体反而比 P1/P3 **更窄更低**，与“离散度扩张”的叙述相反；论文正文如实披露了 IQR/MAD 不变，但图本身并未呈现其核心证据（SD/skew 只以文字标注出现）。**建议：把 P2 的流感季对照限定为同月对齐（10 月对 10 月）或按相位分层；图 (d) 改为显示右尾（如对数轴箱线图或超额峰度/尾部指数对比），或至少不要把该面板题为 “Dispersion across reporting regimes”。**

**W6（重要）实证部分没有真正的预测基准比较。** §5.2-§5.5 评价的“预测”只有一个 $ \widehat C_{t+1|t}=\bar R_{\text{phase}}D_t$ 的持续性型基准，用 RelMSE 评分，没有与论文自己引用的 Cori 更新过程 $R_t$ 估计、也没有与任何时间序列基线做对照。而论文自己证明（式 relmse_identity）该评分与“事后增长统计量”只差常数因子 $1/\bar R^2$，因此实证测的是比值 $C_{t+1}/D_t$ 的离散度，不是预测技能。§5.5 的“全国汇总 RelMSE 0.0348（$N=28$）显著低于任何单州平台（约 0.10）”既没给区间也没做检验，$N=28$ 时“显著”二字没有依据。

**W7（定位）$m_\times$ 的新意需要更准确的刻度。** 定义 1 令 $A/m=v_R$，这正是单期估计的**信度比（随机效应方差占比）等于 0.5** 的位置——即方差成分/测量误差文献中的经典可靠性交叉点，而非新发现的物理边界。论文对“与 $R_t$ 目标下的信息下界对照”着墨很多，却没有与测量误差/随机效应/可靠性这一支文献对话。命题 3 的内容是“$n$ 与 $\rho$ 对 $1/(\rho n)$ 与 $1/(kn)$ 的系数不同”这一初等代数事实；命题 4 的实质是“对 $T$ 个独立期取平均把 $v_R$ 降为 $v_R/T$”，其中 (iii) 项是“可行期数集合”的**定义**而不是结果；命题 4 的 Remarks 也承认真实疫情不允许设计者任意拆分风险集。标题与摘要里的“边界（Boundary）”比正文克制得多的表述要更冲，建议弱化。

**W8（可复现性，多处）文档与代码对不上。**

1. README 把图 1 的随机种子写成 20260924，但 `run_theory_simulations.py:31` 用的是 `seed=20260923`（20260924 是检测模拟 `run_detection_simulations.py:71` 的种子）。
2. README 说 `theory/propositions_and_proofs.md` 是“命题 1-6 的推导底稿”，该文件实际只有 4 个命题，且其“命题 4”是鞅差分解，既不是论文的命题 4（预算权衡）也不是命题 6（预测方差分解）。论文共 6 个命题、5 个证明，**命题 5（增长判定检验与精确离散功效）没有任何证明**（`main.tex`:302-333 后直接接命题 6），其中“负二项族关于 $C_t$ 具有 MLR”与“非随机化上尾检验的复合原假设水平在 $R_t=1$ 取上确界”这两步虽然标准，仍应补一段证明或给出引理出处。
3. 图 2 的 80% 功效样本量（$m_{80\%}=518/142/57/198/415$，正文写作 517.8/142.0/57.2/198.0/415.0）**是绘图函数里的硬编码图例字符串**（`run_detection_simulations.py`:133-136、156-160），`plot_fig2_detection` 只在 120 点网格上算功效，从不做 §4.2(b) 声称的“自 $n=1$ 起以步长 1 递增枚举整数 $n$，取首次满足 $\pi\ge0.80$ 的最小整数 $n$”。我按论文自述规则独立实现该枚举后：$\delta=0.15/0.30/0.50$ 得 $n=2071/568/229$，$k=1.0/0.1$（$\delta=0.25$）得 $n=568/1660$，**五个值与论文一致**；但 $k=0.35,\delta=0.25$ 的首次穿越是 $n=790$（$m=197.5$），不是文中/图中的 792（198.0）。即数字大体正确但生成方式与自述不符，且其中一个不是“首次穿越”。**请把枚举写进脚本并让图例由计算结果驱动。**
4. §5.4 的两个子段统计量（法定强制子段 $N=3{,}382$、覆盖率 93.7%、标准差 0.620、偏度 3.45；早期高覆盖子段标准差 0.628）**不在 `empirical_stats.json` 里，也没有任何脚本计算它们**——我按 $D_t\ge5$ 口径独立重算得到完全相同的值（2627/87.8%/0.628、3382/93.7%/0.620/3.45），说明数字无误，但与 README“`empirical_stats.json` 是论文第 5 节全部数字的唯一数据来源”的声明冲突。
5. “滞后基准重估：流感季中位数 19.7 对非流感季 47.9”**无法用仓库现有代码复现**：`run_empirical_flu_analysis.py` 的滚动段只做窗口内基准，`m_x_rolling` 也只存 `R_bar_w`。（窗口内基准的 20.4 对 46.9 我可以从 JSON 直接导出，与论文一致。）请补上滞后基准那一段代码与输出字段。
6. README 写 `main.pdf` 为 20 页，`pdfinfo` 实测 **22 页**。
7. `main.tex`:564、566 两处 `\textbf` 被写成了制表符加 `extbf`（`其<TAB>extbf{法定强制子段}`）。经 `pdftotext` 核对，**权威交付件 `main.pdf` 中这两处把字面字符串 “extbf” 排进了正文**（第 11 页附近：“偏度 3.29；其 extbf 法定强制子段”）。这是投稿时会被直接看到的排版错误。
8. `references.bib` 的 `parag2025asymmetric` 条目有误：文章号写 8:34，实际为 Communications Physics 8, Article **450**（DOI 10.1038/s42005-025-02358-w，2025-11-19，已用 Crossref 核对），且漏了第四作者 Sandor Beregi。Word 版参考文献表沿用了同一错误。全部 11 条条目均无 DOI 字段。
9. `run_empirical_flu_analysis.py:51` 对 `Total Influenza Admissions` 做 `fillna(0)`：504 个州--周（P1 468 个、P2 36 个）的缺失被当作 0 例。§5.1 未披露该处理，而“未填报”与“零住院”在制度切换期恰恰不能等同。

**W9（次要）** §5.1 说第三阶段“至样本终点 2026 年 8 月”，数据实际到 2026-09-05；§4.1 称 Wald 检验“全低规模段（$m<40$）系统性过检”，但我调用脚本内部结果看到 $k=1.0$ 时 $m=5$、$m=20$ 的经验一类错误率是 0.0495、0.0500，并未过检，该概括应限定在 $k\le0.35$；图 2(b)(c) 把精确检验（实际水平 0.045-0.050，保守）与 Wald（最高 0.081）的功效曲线并列，两者不同水平，论文没有给出同水平竞争检验（如 mid-p 或校正方差后的 Wald）以量化“精确性”的功效代价；四张图的轴标签、图例与图题均为英文而正文为中文（ctex），`fig4` 面板 (a) 的 “P2 Voluntary” 与 “P3 Re-mandated” 文本框互相重叠。

**W10（次要）** `figures/fig4` 面板 (b) 的注释与竖线写的是 “diminishing returns beyond ~50 cases/week”，并在 `run_empirical_flu_analysis.py`:663 处以 `ax.axvline(2, ...)` 硬编码在 $[20,50)$ 与 $[50,100)$ 之间，而 §5.2 与摘要给出的中心量是 22.6 例/周（落在 $[20,50)$ 箱内部）。同一张图与正文对同一个量给出两个数，需统一。

## 四、详细意见（Detailed Comments）

**A. 理论与命题**

A1. 命题 1(iv) 声明“严格达到 Cramér–Rao 下界”。证明给的是 $\mathrm{Var}(\partial_{R_t}\ln L)=\mathcal I$，这在正确设定的指数族下成立，我复核无误。但 (ii) 中 $C_t=0$ 时 $\widehat R_t=0$ 位于参数空间边界，此时有效性论证不成立——建议把 (iv) 明确限定在内部点并以 (ii) 的边界注记承接。

A2. 命题 2 与定义 1 把 $v_R$ 称作“方差底板”。$v_R$ 是**指定单期估计量**的方差成分，不是关于 $\bar R$ 的信息下界（命题 4 恰恰说明 $T$ 期可把 $1/T$ 因子压上去，没有下界）。正文 Remarks 已说明，但摘要中“方差受环境底板 $v_R$ 制约”仍易被读成下界。建议摘要改为“在单期设计下受 $v_R$ 制约”。

A3. 命题 4 的 (i) “增加 $T$ 严格单调降低估计方差”在 $v_R>0$ 与跨期独立下是恒真；AR(1) 版本下 $T_{\text{eff}}(T,r)=T(1-r)/(1+r)$ 的形式也是标准结果。建议把 (iii)（可行期数 = $B/\rho$ 的正整数因子集）从命题正文降为注记，它不是统计结果。

A4. 命题 5 缺证明（见 W8-2）。另外：(i) 的水平控制在 $R_t=1$ 取上确界依赖 MLR，而 (ii) 的功效式是精确计算——建议补一句说明 $c_\alpha$ 作为 $n$ 的函数不单调、因而 $\pi(n,\delta)$ 关于 $n$ 非单调（这正是我复现出 790 与 792 差异的原因），并把它作为“必须枚举而非二分”的理由写进 §4.2(b)。

**B. 模拟与图**

B1. 见 W1：删除 `run_forecasting_crossover_simulations.py:88` 的 `e < 5.0` 截尾，或将其改造成明文声明的阈值并同时给出未截尾结果；`I[t] = max(1, I_t)` 需在 §4.2 说明并给出敏感性。

B2. 图 2 的 $m_{80\%}$ 改为计算产物（W8-3），并在图注标明“首次穿越”而非“网格上的某点”。

B3. 图 1 的验证比 0.988-1.017 与 $m_\times=53.08$ 我独立复算一致（$A=2.1232$，$m_\times=53.080$；$m=10/53.1/200$ 的翻倍削减 42.07%/25.0%/10.49% 与图注 42.1%/25.0%/10.5% 一致）。这一组是最扎实的。

B4. 图 3 面板 (c) 的经验翻倍削减直接取自已截尾的 `growth_mse`，修正 B1 后该曲线的 $m=6$ 附近会明显改变，请重跑并核对“红圈紧贴深蓝实线”的结论是否仍成立。

**C. 实证与统计**

C1. 见 W3：把截距的来源拆开。最直接的检验是把论文自己引用的状态空间/更新过程模型（Bracher & Held；Bhatt 等；Steyn 等）套到 $C_t$ 上，看残差的自相关结构能否解释 $a- v_R/\bar R^2$ 的差额。若不能，则“环境协变量”这一政策推论需要撤回。

C2. 关于 $b$ 的量级：机制隐含值约 0.6-0.8（用论文自述的 $\rho$、$k$）而估计值 2.2434。最可能的漏项是论文自己写在式 (exact_renewal_decomp) 里的 $(\mu_{t+1|t}/D_t-\bar R)^2$——它在快速上升波段与 $D_t$ 同向，会被 $1/D_t$ 回归吸收。建议直接把该项算出来并在仿射拟合中扣除，看 $b$ 是否落到机制量级。这是本稿最容易补强、也最能决定结论强度的一步。

C3. FGLS 的权重 $1/\hat\mu^2$ 是启发式的（论文以 $\mathrm{Var}(L)/\mathbb E[L]^2$ 从 5.32 降到 0.99 作为依据）。既然论文已报告 OLS 与 FGLS 在交互项上的差异（2.3049 对 2.2434），建议把权重选择也纳入稳健性表，并说明若真实条件方差模型不同，FGLS 点估计的一致性如何受影响。

C4. 变量误差：论文用滞后 3-5 周核构造 $D_t^{\text{iv}}$ 做 2SLS，并诚实承认排除限制未验证。但第 1 阶段回归的是 $1/D_t$ 对 $1/D_t^{\text{iv}}$，工具与误差项的相关性在强季节趋势下很可能不为零；而 Word 版直接把排除限制说成成立（见 W2）。建议：要么补一个季节内/相位内的子样本 IV，要么统一按 LaTeX 的谨慎表述。

C5. 滚动窗口：251 个 26 周窗口高度重叠（步长 1 周），论文已指出 IQR 的有效样本量小于表观窗口数，但没有给任何修正。建议改用非重叠或按块 bootstrap 的窗口并给出区间。

C6. 详见 W5（政策期混杂）、W6（无预测基准、全国 $N=28$ 的“显著”）。

C7. §5.1 的活跃周阈值 $D_t\ge5$ 与“不设覆盖率门槛”的理由（避免系统性剔除低覆盖观测）是合理的，但 `fillna(0)` 与 $D_t$ 的测量误差共同作用在 P2 期最严重（36 个缺失 + 覆盖率 54.6%）。建议至少给出一个把 $D_t$ 阈值改为 10 或 20 的敏感性。

C8. 一个值得写进正文的小结论：论文自述的阶段间 $b$ 比较在共同分母下“差异不显著”，这与图 4(c) 中“达峰期误差曲线几乎水平”是同一致的；建议在 §5.3 明写“我们未能检出阶段间规模效应的差异，能检出的只是基准偏移”，避免读者从图的水平形态反推“规模失效”。

**D. 交付物与可复现性**

D1. 见 W8 全部九条：种子、命题数、硬编码图例、未落盘的 §5.4 子段统计量、无法复现的滞后基准、页数、`extbf` 排版错误、文献条目错误、`fillna(0)`。

D2. `main_docx.docx` 与 `main.tex` 是两份结构、编号、内容都不一致的文档（docx 有 LaTeX 没有的“研究回顾”节，LaTeX 有 docx 没有的表 2/表 3、图 1 与全部稳健性检验）。若两者都要交付，建议明确各自定位（例如 docx 只作排版模板样张并删除会导致独立解读的正文），否则应以同一内容真源生成。

D3. `figures/` 与 `paper/` 的图片路径用 `../figures/...`，从 `paper/` 编译可通；建议在 README 加上“从 `paper/` 目录调用 tectonic main.tex”，并声明编译产物的页数与校验方式（例如与仓库 `main.pdf` 的 sha256 对比），使“20 页”这类失配不再发生。

**E. 写作与文献**

E1. 参考文献只有 11 条，且方法论线索高度集中于同一批作者（Parag 等 3 条）。对一篇以 CDC NHSN 报告制度切换为核心实证的论文，缺少 2024 年 NHSN 强制/自愿填报变更与住院监测覆盖率的相关文献，也缺少测量误差/随机效应可靠性一支（与 W7 直接相关），以及流感住院预测评估（FluSight 一类）的对照工作。

E2. 除 W8-8 的条目错误外，建议为全部条目补 DOI，并核对 `parag2022quantifying`（Nature Computational Science 2(9):584-594）与 `aber2025timeseries`（PLOS Comput Biol 21(2):e1012882）的卷期页。

E3. 术语：“方差占比交叉点”“方差底板”“误差预算分解”是作者自造名词，建议首次出现时给出与既有术语的对应（信度比 / 随机效应方差 / 方差成分分解），便于跨领域读者定位。

E4. 全稿未见直角引号违规，术语与符号一致性良好，LaTeX 的定理环境命名清晰，这些是可取的。

**给作者的核心问题清单**

1. 图 3 的 `e < 5.0` 截尾是何时加入的？未截尾的曲线是否仍支持“小规模端经验误差低于单代参照”这一论述？
2. 截距 $a=0.0991$ 中，有多少来自 $v_R$、多少来自未观测 $\Lambda$ 的代理不确定性？能否用论文自己引用的状态空间模型分离？
3. 若按机制隐含值（$b\approx0.6$--$0.8$）与估计值（2.2434）的差额，主要由 $(\mu/D_t-\bar R)^2$ 解释，那么“经验交叉点 22.6”是否还值得写进摘要？
4. Word 交付件为何保留了 LaTeX 明确撤销的“达峰期规模项绝对量更高”？两份交付件以哪一份为准？
5. 在没有任何竞争预测基准、且评分与事后统计量代数同一个量的情况下，§5 的经验结果支持的是“预测精度随规模衰减”，还是“比值 $C_{t+1}/D_t$ 的离散度随规模衰减”？

## 五、总体推荐（Overall Recommendation）

**推荐：Major Revision（大修）。**

**评分：4 / 10。**

理由：理论部分正确但初等——命题 1-4 与命题 6 的推导我逐条复核无误，命题 3 的杠杆非对称性也是真实的机制事实，但整体是把“测量误差 + 随机效应方差成分”的标准分解换上了监测语言；$m_\times$ 实为单期信度比等于 0.5 的位置，作者未与相应文献对话，标题中的“边界”强于正文所能支持的断言。实证部分的数据处理与管线质量很高（确定性可复现、头条数字全部可从原始 CSV 复算、主动撤销对自己有利的比较），这是本稿真正的价值所在；但三项核心结论各自带有实质缺陷：误差预算的“环境”份额在论文自己的更新过程代数下并未被识别为环境项（截距应含未观测感染压力的不确定性，机制隐含的截距只有估计值的 1/3-1/7）；份额分解依赖被作者自己证明“偏乐观”的回顾性基准（换成诚实基准后 $a$、$b$ 变 4-6 倍）；而图 3 存在一行未披露的、恰好决定小规模端结论的数据依赖截尾，使“平滑追踪参照曲线”的论述在完整数据下不成立。加上 Word 交付件与 LaTeX 权威版在一个已被作者自己推翻的结论上互相矛盾、多处稳健性论断在 Word 版中被反向表述，以及九处文档/代码失配（含权威 PDF 中的 `extbf` 排版错误与一条错误的参考文献），本稿在当前状态下不宜发表。

这些都是**可修复**的问题：删掉截尾并重跑、把截距的来源拆开、把份额分解改为滚动口径并表、让 Word 版与 LaTeX 结论一致、把硬编码的 $m_{80\%}$ 与子段统计量写进脚本、补上命题 5 的证明与 DOI、统一图 4 面板 (b) 的交叉点标注。若上述修订到位，凭借数据质量与作者已展现出的自我纠正能力，本稿有机会成为一篇扎实的监测设计论文。

---

## 参考与链接

[1] 被评审仓库主页. https://github.com/wangchi2068/case-count-inference-bottleneck

[2] 论文权威源文件 `paper/main.tex`. https://github.com/wangchi2068/case-count-inference-bottleneck/blob/main/paper/main.tex

[3] 预测模拟脚本 `simulations/run_forecasting_crossover_simulations.py`（W1 的截尾位于第 88 行）. https://github.com/wangchi2068/case-count-inference-bottleneck/blob/main/simulations/run_forecasting_crossover_simulations.py

[4] 实证分析脚本 `simulations/run_empirical_flu_analysis.py`. https://github.com/wangchi2068/case-count-inference-bottleneck/blob/main/simulations/run_empirical_flu_analysis.py

[5] 检测功效脚本 `simulations/run_detection_simulations.py`（硬编码 $m_{80\%}$ 图例位于第 133-136、156-160 行）. https://github.com/wangchi2068/case-count-inference-bottleneck/blob/main/simulations/run_detection_simulations.py

[6] Word 交付件 `paper/main_docx.docx`. https://github.com/wangchi2068/case-count-inference-bottleneck/blob/main/paper/main_docx.docx

[7] 理论底稿 `theory/propositions_and_proofs.md`. https://github.com/wangchi2068/case-count-inference-bottleneck/blob/main/theory/propositions_and_proofs.md

[8] 参考文献条目 `parag2025asymmetric` 的正确出版信息（Communications Physics 8, Article 450, 2025；作者含 Sandor Beregi）. https://doi.org/10.1038/s42005-025-02358-w

## 核验范围与局限

- 仓库可正常 `git clone`，README、`main.tex`、`main.pdf`、`main_docx.docx`、理论底稿、四个脚本与数据文件均已获取，**没有不可达的源**，故本评审不含“源不可达”条目。
- 我完整重跑的是 `run_empirical_flu_analysis.py`；理论模拟与预测模拟我通过重算核心循环/直接调用其函数来核验（含独立重写功效枚举与预测核心），未整体重跑绘图脚本。图 2、图 4 为直接查看仓库内 PNG 所得，图 1、图 3 的数值结论以解析复算为准。
- 未尝试编译 LaTeX（环境无 Tectonic/ctex），`main.pdf` 的页数与正文内容以 `pdfinfo`、`pdftotext` 的抽取结果为准。
- 论文自述的住院参数（$\rho\approx0.01$--$0.05$、$k\approx0.2$--$0.5$、$v_R\approx0.04$--$0.08$）系作者给出的“典型参数空间”，并非识别结果；第三、四节的机制一致性论证（W3、C2）建立在该区间之上，属一致性检查而非参数识别，作者若给出更可靠的参数区间，相应结论需重新评估。
